# 需求索引

## draft

- `macos-desktop-distribution` - 让 PixAI 能构建、分发并更新 macOS 桌面版本。

## current

- `app-update` - 让用户随时知道应用是不是最新版本，并能一键检查更新。
- `canvas-assistant` - 在 Canvas 里用一句话安排节点、连线、提示词和生成运行。
- `reference-image-input` - 把图片贴进、拖进、通过链接导入，也能继续放进 Canvas 节点化创作。

## outdated

- 无
